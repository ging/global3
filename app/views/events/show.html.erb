<% content_for :sidebar do %>
  <%= render :partial => 'home/sidebar' %>
<% end %>

<%= location(link_to(image_tag("btn/btn_event.png", :class => "btn_config")+t('home')+": "+
                      content_tag(:span, ( sanitize current_subject.name), :class=>"name_group")))%>

<% toolbar %>

<br class="clearfloat" />
<div class="space_center">
</div>


<%= render :partial => "activities/index",
           :locals => { :activities => current_subject.wall(:home).page(params[:page]),
                        :owner => current_subject } %>


