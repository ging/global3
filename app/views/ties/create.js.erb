<% if @tie.new_record? %>
  alert('There were problems with the tie: <%= escape_javascript(@tie.errors.to_xml) %>');
<% else %>
  $(".close").click();
  $("#spaces_list").prepend("<%= escape_javascript(render @tie.receiver_subject) %>");
<% end %>